{
  "name": "@vendflow/database",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "@auth/prisma-adapter": "^2.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
